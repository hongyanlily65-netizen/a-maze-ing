[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
description = "Reusable randomized Prim maze generator"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
include = ["mazegen*"]
